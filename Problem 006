#include <bits/stdc++.h>
#define PI 3.14159265359
using namespace std;
const int64_t MOD = 1e9 + 7;

int main() {
	int N = 100;

	int64_t n = 0;
	for (int i = 1; i <= N; i++) {
		n += i * i;
	}

	int64_t m = (N + 1) * N / 2;
	m *= m;

	cout << abs(n - m) << endl;
}
